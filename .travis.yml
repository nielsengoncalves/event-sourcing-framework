language: java
jdk:
  - oraclejdk8
sudo: true
services:
  - docker
before_install:
 - docker-compose pull
 - docker-compose build
 - docker-compose start
 - docker ps
install: true
script:
 - mvn clean install
after_success:
 - bash <(curl -s httpmvn s://codecov.io/bash)